{
    "name": "nextpress",
    "scripts": {
        "dev": "npm install --prefix ./next-js && cross-env NODE_ENV=development docker compose up --watch --build --menu=0; npm run stop",
        "start": "cross-env NODE_ENV=production docker compose up --build",
        "logs": "docker compose logs -f",
        "stop": "docker compose stop",
        "remove": "docker compose down",
        "wp": "docker compose run --rm wp-cli wp",
        "generate-db-types": "docker compose exec next-js npm run kysely-codegen"
    },
    "devDependencies": {
        "cross-env": "^10.1.0"
    }
}
