{
    "name": "nextpress",
    "scripts": {
        "dev": "npm install --prefix ./next-js & docker compose up --watch --build --menu=0; npm run stop",
        "start": "docker compose up --build",
        "logs": "docker compose logs -f",
        "stop": "docker compose stop",
        "remove": "docker compose down",
        "wp": "docker compose run --rm wp-cli wp",
        "generate-db-types": "docker compose exec next-js npm run kysely-codegen"
    }
}
