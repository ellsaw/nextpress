import nextpressNextConfig from "@nextpress/nextpress.next.config";
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    ...nextpressNextConfig
    /* config options here */
};

export default nextConfig;
